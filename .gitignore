# deps
node_modules/
.pnpm-store/

# builds
dist/
dist-ssr/
*.local

# fumadocs (runs as its own Next server on :3001, proxied by the web app)
apps/docs/.next/
apps/docs/.source/

# vite
.vite/
vite.config.*.timestamp-*

# tsc
*.tsbuildinfo

# editors / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# env
.env
.env.local
.env.*.local

# logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# turbo / misc
.turbo/
.vercel/

# playwright-cli artifacts
.playwright-cli/

# railcode deploy sync state (per-folder; never commit)
.railcode

# audio / video / captions (never commit binary media or transcripts)
*.mp3
*.mp4
*.srt

# documents (never commit binary office files)
*.docx
