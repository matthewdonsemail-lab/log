---
title: Listings
description: Facebook Marketplace listings (mock of facebook-camofox-client).
full: true
_openapi:
  toc:
    - depth: 2
      title: List listings
      url: '#list-listings'
    - depth: 2
      title: Create listing
      url: '#create-listing'
    - depth: 2
      title: Update listing
      url: '#update-listing'
    - depth: 2
      title: Delete listing
      url: '#delete-listing'
    - depth: 2
      title: Get listing status
      url: '#get-listing-status'
    - depth: 2
      title: Update listing status
      url: '#update-listing-status'
  structuredData:
    headings:
      - content: List listings
        id: list-listings
      - content: Create listing
        id: create-listing
      - content: Update listing
        id: update-listing
      - content: Delete listing
        id: delete-listing
      - content: Get listing status
        id: get-listing-status
      - content: Update listing status
        id: update-listing-status
    contents:
      - content: >-
          Lists all Marketplace listings — seeded `MOCK_LISTINGS` plus any
          user-created rows (persisted to localStorage, migrated for legacy
          `accountId` FKs). Each has `listingId`, title/price/location, 1–4
          images (first is cover), `accountId` FK, `locationPoint` (lat/lng +
          radius 1–200km), and `status`. This is the source for the dashboard
          Listings table. Code: apps/web/src/lib/listings/server.ts:77
        heading: list-listings
      - content: >-
          Creates a new Marketplace listing. Validates `title`, `price`,
          `location` (all trimmed non-empty), at least one `images` string, and
          a Facebook `accountId` (resolves legacy label via
          `resolveListingAccountId`, falls back to `fb-personal`). Generates a
          17-digit `listingId`, sets `category`/`condition` defaults,
          `locationPoint` clamped 1–200km, `status: under-review`, and
          `listingUrl`. Persists and returns 201. Code:
          apps/web/src/lib/listings/server.ts:79
        heading: create-listing
      - content: >-
          Patches a listing's details/photos in place — `listingId`,
          `listingUrl`, `publishedAt`, and `status` stay put; only `title`,
          `price`, `location`, `category`, `condition`, `images` (sliced to 4),
          `locationPoint`, and `accountId` (resolves legacy label or keeps
          stored FK) move. Validates title/price/location + photo presence, 404
          if unknown. Code: apps/web/src/lib/listings/server.ts:123
        heading: update-listing
      - content: >-
          Hard-deletes `:listingId` — the row leaves the store entirely (unlike
          the `removed` status which keeps it visible for record-keeping).
          Persists and returns the remaining list. 404 if unknown. Code:
          apps/web/src/lib/listings/server.ts:192
        heading: delete-listing
      - content: >-
          Returns the lightweight status probe for `:listingId` — `{ listingId,
          status, title }` (status typed as `ListingStatus`). Used by the
          dashboard to poll `under-review → active` transitions without fetching
          the full record. Code: apps/web/src/lib/listings/server.ts:183
        heading: get-listing-status
      - content: >-
          Transitions a listing's `status` via the status route — never local
          state. The dashboard's Sold/Remove actions round-trip here so the mock
          stays the source of truth and the live facebook-camofox-client can
          implement the same shape. Validates `status` against
          `LISTING_STATUSES`; 404 if unknown id. Code:
          apps/web/src/lib/listings/server.ts:166
        heading: update-listing-status
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

## Listings — Facebook Marketplace items

Listings are Marketplace rows the dashboard creates and polls for status. Each has `listingId` (17-digit marketplace shape), title/price/location, up-to-4 images (first is cover), `accountId` FK to a Facebook account, `locationPoint` (lat/lng + radius 1–200km), and `status` (`under-review` at creation). The store seeds from `MOCK_LISTINGS` and persists.

Six routes: list, create (validates title/price/location/photo/account), patch details, patch status (transitions via `LISTING_STATUSES`), get status, and hard-delete. Code: `apps/web/src/lib/listings/server.ts`.
### `GET /listings` — List listings

Lists all Marketplace listings — seeded `MOCK_LISTINGS` plus any user-created rows (persisted to localStorage, migrated for legacy `accountId` FKs). Each has `listingId`, title/price/location, 1–4 images (first is cover), `accountId` FK, `locationPoint` (lat/lng + radius 1–200km), and `status`. This is the source for the dashboard Listings table. Code: apps/web/src/lib/listings/server.ts:77

### `POST /listings` — Create listing

Creates a new Marketplace listing. Validates `title`, `price`, `location` (all trimmed non-empty), at least one `images` string, and a Facebook `accountId` (resolves legacy label via `resolveListingAccountId`, falls back to `fb-personal`). Generates a 17-digit `listingId`, sets `category`/`condition` defaults, `locationPoint` clamped 1–200km, `status: under-review`, and `listingUrl`. Persists and returns 201. Code: apps/web/src/lib/listings/server.ts:79

### `PATCH /listings/{listingId}` — Update listing

Patches a listing's details/photos in place — `listingId`, `listingUrl`, `publishedAt`, and `status` stay put; only `title`, `price`, `location`, `category`, `condition`, `images` (sliced to 4), `locationPoint`, and `accountId` (resolves legacy label or keeps stored FK) move. Validates title/price/location + photo presence, 404 if unknown. Code: apps/web/src/lib/listings/server.ts:123

### `DELETE /listings/{listingId}` — Delete listing

Hard-deletes `:listingId` — the row leaves the store entirely (unlike the `removed` status which keeps it visible for record-keeping). Persists and returns the remaining list. 404 if unknown. Code: apps/web/src/lib/listings/server.ts:192

### `PATCH /listings/{listingId}/status` — Update listing status

Transitions a listing's `status` via the status route — never local state. The dashboard's Sold/Remove actions round-trip here so the mock stays the source of truth and the live facebook-camofox-client can implement the same shape. Validates `status` against `LISTING_STATUSES`; 404 if unknown id. Code: apps/web/src/lib/listings/server.ts:166

### `GET /listings/{listingId}/status` — Get listing status

Returns the lightweight status probe for `:listingId` — `{ listingId, status, title }` (status typed as `ListingStatus`). Used by the dashboard to poll `under-review → active` transitions without fetching the full record. Code: apps/web/src/lib/listings/server.ts:183


<APIPage document={"openapi.json"} operations={[{"path":"/listings","method":"get"},{"path":"/listings","method":"post"},{"path":"/listings/{listingId}","method":"patch"},{"path":"/listings/{listingId}","method":"delete"},{"path":"/listings/{listingId}/status","method":"get"},{"path":"/listings/{listingId}/status","method":"patch"}]} webhooks={[]} hasHead={true} />